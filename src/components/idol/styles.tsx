import { StyleSheet } from "react-native";
import { Theme } from "../../theme/theme";

export const IdolStyles = (theme: Theme) =>
  StyleSheet.create({
    container: {
      height: 13,
      width: 13,
      marginLeft: 10,
      marginTop: 5,
    },
  });
